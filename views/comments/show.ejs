<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Showing comment</title>

    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f9f9f9;
        text-align: center;
        padding: 20px;
      }
      h1 {
        color: #333;
      }
      h2 {
        color: #555;
      }
      p {
        font-size: 18px;
        color: #666;
        background: #fff;
        padding: 15px;
        border-radius: 8px;
        display: inline-block;
        box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
      }
      a {
        text-decoration: none;
        color: white;
        background: #007bff;
        padding: 10px 15px;
        border-radius: 5px;
        margin: 10px;
        display: inline-block;
      }
      a:hover {
        background: #0056b3;
      }
      form {
        margin-top: 15px;
      }
      button {
        background: #dc3545;
        color: white;
        border: none;
        padding: 10px 15px;
        border-radius: 5px;
        cursor: pointer;
      }
      button:hover {
        background: #a71d2a;
      }
    </style>
  </head>
  <body>
    <h1>Comment id: <%- comment.id %></h1>
    <h2><%- comment.username %></h1>
    <p><%- comment.comment %></p>
    <a href="/comments/<%-comment.id%>/edit">Edit</a>
    <form method="post" action="/comments/<%-comment.id%>/?_method=DELETE">
      <button>Delete</button>
    </form>
    <a href="/comments">Go to home</a>
  </body>
</html>